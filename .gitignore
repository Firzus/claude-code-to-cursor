# dependencies
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz
.pnpm-store/

# next.js
/.next/
/out/
/build/
*.tsbuildinfo
next-env.d.ts

# convex: `_generated/` IS committed so CI can typecheck/build without
# a live Convex backend (`convex codegen` requires admin auth). Pure type
# bindings, no secrets — regenerated locally by `pnpm run convex:deploy`.

# tests / playwright
/coverage
/playwright-report
/test-results
/playwright/.cache

# env (secrets)
.env*
!.env.example

# logs
*.log
api.log
cctc-startup.log
cloudflared-startup.log

# ide / os
.DS_Store
Thumbs.db
desktop.ini
*.pem
.idea/
.vscode/
.vercel
.worktrees/
